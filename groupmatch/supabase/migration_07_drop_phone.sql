-- =============================================================
-- Migration 07 — 전화번호 컬럼 제거
-- 전화번호는 수집하지 않기로 하여 applicants.phone 삭제.
-- =============================================================

alter table applicants drop column if exists phone;
