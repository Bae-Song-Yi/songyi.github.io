-- =============================================================
-- Migration 06 — 전화번호 필드 추가
-- 신청 완료 알림(추후 알림톡/문자) 발송용 연락처
-- ⚠️ 전화번호는 민감 개인정보 → 수집 동의 안내와 함께 운영 권장
-- =============================================================

alter table applicants add column if not exists phone text;
