-- =============================================================
-- Migration 09 — 회사 이메일 인증 컬럼
-- 신청 전 회사 이메일을 OTP로 인증하고, 인증된 이메일을 저장.
-- =============================================================

alter table applicants add column if not exists email text;
